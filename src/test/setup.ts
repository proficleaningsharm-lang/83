// Vitest global setup — jsdom environment provides DOM APIs.
// jest-dom adds DOM-specific matchers (toBeInTheDocument, etc.) used by
// SignalFeed.test.tsx / PredictionAccuracyBadge.test.tsx.
import '@testing-library/jest-dom/vitest';
